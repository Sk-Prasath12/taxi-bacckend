# Taxi Backend

Node.js API for the Taxi Customer, Driver, and Admin apps.

## Backend code

The main project lives in [`taxi-backend-main/`](taxi-backend-main/).

```bash
cd taxi-backend-main
npm install
cp .env.example .env.local
npm run dev
```

See `taxi-backend-main/.env.example` for required environment variables.
